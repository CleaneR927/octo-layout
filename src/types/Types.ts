export interface ICard {
  image: string;
  categoryColor?: string;
  categoryName?: string;
  authorColor?: string;
  authorName?: string;
  title: string;
  text: string;
  buttonColor: string;
  buttonText: string;
}
